{
  "name": "TransferBuddy.Service",
  "version": "1.0.0",
  "description": "",
  "scripts": {
      "preinstall": "npm list bower -g || npm install bower -g",
      "postinstall": "bower cache clean"
  },
  "dependencies": {
  }
}
